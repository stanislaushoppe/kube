apiVersion: apps/v1
kind: Deployment
metadata:
  name: stan-nginx-deployment
spec:
  selector:
    matchLabels:
      app: nginx
  replicas: 4
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:10.25.3
        ports:
        - containerPort: 80
